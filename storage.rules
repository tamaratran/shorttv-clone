rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for all files (serving video content)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
